/*
	Similar to joins but combines vertically.
    No. of columns selected must be same from each table.
	1.Union
    2.Intersect(Emulated in MySQL using "INNER JOIN")
    3.Minus(Emulated in MySQL using "LEFT JOIN")
*/